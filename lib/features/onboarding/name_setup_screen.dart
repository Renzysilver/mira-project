import 'package:flutter/material.dart';
class NameSetupScreen extends StatelessWidget {
  const NameSetupScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Name Setup - Integrated in Onboarding')));
}
