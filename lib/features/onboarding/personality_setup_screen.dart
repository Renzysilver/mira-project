import 'package:flutter/material.dart';
class PersonalitySetupScreen extends StatelessWidget {
  const PersonalitySetupScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Personality Setup - Integrated in Onboarding')));
}
